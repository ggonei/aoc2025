{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "command": "cobc",
      "args": [
        "-x",
        "${file}"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "Run",
      "type": "shell",
      "command": "${workspaceFolder}/${fileBasenameNoExtension}",
      "group": "build",
      "dependsOn": "Build"
    }
  ]
}
